<%- include('partials/header', { currentPage: 'settings', pageTitle: 'Settings' }) %>

<div class="grid grid-2" style="gap: 24px;">
  <!-- Profile Settings -->
  <div class="card">
    <div class="card-header">
      <h3 class="card-title">Profile Information</h3>
    </div>
    
    <form id="profileForm">
      <div class="form-group">
        <label class="form-label">Company Name</label>
        <input type="text" name="companyName" class="form-input" id="companyName">
      </div>
      
      <div class="form-group">
        <label class="form-label">Company Code</label>
        <input type="text" name="companyCode" class="form-input" id="companyCode" readonly style="background: var(--bg-main);">
      </div>
      
      <div class="form-group">
        <label class="form-label">Contact Name</label>
        <input type="text" name="contactName" class="form-input" id="contactName">
      </div>
      
      <div class="form-group">
        <label class="form-label">Email Address</label>
        <input type="email" name="email" class="form-input" id="email" readonly style="background: var(--bg-main);">
      </div>
      
      <div class="form-group">
        <label class="form-label">Phone Number</label>
        <input type="tel" name="phone" class="form-input" id="phone">
      </div>
      
      <div class="form-group">
        <label class="form-label">Website</label>
        <input type="url" name="website" class="form-input" id="website" placeholder="https://">
      </div>
      
      <button type="submit" class="btn btn-primary">
        <i data-lucide="save" style="width: 18px; height: 18px;"></i>
        Save Changes
      </button>
    </form>
  </div>
  
  <!-- Security Settings -->
  <div class="card">
    <div class="card-header">
      <h3 class="card-title">Security</h3>
    </div>
    
    <form id="passwordForm">
      <div class="form-group">
        <label class="form-label">Current Password</label>
        <input type="password" name="currentPassword" class="form-input" placeholder="Enter current password">
      </div>
      
      <div class="form-group">
        <label class="form-label">New Password</label>
        <input type="password" name="newPassword" class="form-input" placeholder="Enter new password" minlength="8">
      </div>
      
      <div class="form-group">
        <label class="form-label">Confirm New Password</label>
        <input type="password" name="confirmPassword" class="form-input" placeholder="Confirm new password">
      </div>
      
      <button type="submit" class="btn btn-primary">
        <i data-lucide="lock" style="width: 18px; height: 18px;"></i>
        Change Password
      </button>
    </form>
    
    <hr style="margin: 24px 0; border: none; border-top: 1px solid var(--border);">
    
    <div>
      <h4 style="font-size: 14px; font-weight: 600; margin-bottom: 12px;">Two-Factor Authentication</h4>
      <p style="font-size: 13px; color: var(--text-secondary); margin-bottom: 16px;">
        Add an extra layer of security to your account by enabling two-factor authentication.
      </p>
      <button class="btn btn-secondary" disabled>
        <i data-lucide="smartphone" style="width: 18px; height: 18px;"></i>
        Enable 2FA (Coming Soon)
      </button>
    </div>
  </div>
</div>

<!-- Account Usage -->
<div class="card" style="margin-top: 24px;">
  <div class="card-header">
    <h3 class="card-title">Account Usage</h3>
  </div>
  
  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 24px;">
    <div>
      <div style="font-size: 13px; color: var(--text-secondary); margin-bottom: 8px;">Total Parts</div>
      <div style="font-size: 24px; font-weight: 700;" id="usageParts">0</div>
      <div style="font-size: 12px; color: var(--text-secondary);">uploaded</div>
    </div>
    
    <div>
      <div style="font-size: 13px; color: var(--text-secondary); margin-bottom: 8px;">Import Files</div>
      <div style="font-size: 24px; font-weight: 700;" id="usageFiles">0</div>
      <div style="font-size: 12px; color: var(--text-secondary);">active</div>
    </div>
    
    <div>
      <div style="font-size: 13px; color: var(--text-secondary); margin-bottom: 8px;">Unique Brands</div>
      <div style="font-size: 24px; font-weight: 700;" id="usageBrands">0</div>
      <div style="font-size: 12px; color: var(--text-secondary);">in catalog</div>
    </div>
    
    <div>
      <div style="font-size: 13px; color: var(--text-secondary); margin-bottom: 8px;">In Stock</div>
      <div style="font-size: 24px; font-weight: 700;" id="usageInStock">0</div>
      <div style="font-size: 12px; color: var(--text-secondary);">available</div>
    </div>
  </div>
</div>

<!-- Need Help Section -->
<div class="card" style="margin-top: 24px;">
  <div class="card-header">
    <h3 class="card-title">Need Help?</h3>
  </div>
  
  <div style="display: flex; gap: 24px; flex-wrap: wrap;">
    <div style="flex: 1; min-width: 200px;">
      <div style="font-weight: 600; margin-bottom: 8px;">
        <i data-lucide="mail" style="width: 18px; height: 18px; vertical-align: -4px; margin-right: 8px;"></i>
        Contact Support
      </div>
      <p style="font-size: 13px; color: var(--text-secondary); margin-bottom: 12px;">
        Have questions or need assistance? Our support team is here to help.
      </p>
      <a href="mailto:support@partsform.com" class="btn btn-secondary">
        <i data-lucide="send" style="width: 16px; height: 16px;"></i>
        Email Support
      </a>
    </div>
    
    <div style="flex: 1; min-width: 200px;">
      <div style="font-weight: 600; margin-bottom: 8px;">
        <i data-lucide="book-open" style="width: 18px; height: 18px; vertical-align: -4px; margin-right: 8px;"></i>
        Documentation
      </div>
      <p style="font-size: 13px; color: var(--text-secondary); margin-bottom: 12px;">
        Learn how to get the most out of your supplier portal.
      </p>
      <button class="btn btn-secondary" disabled>
        <i data-lucide="external-link" style="width: 16px; height: 16px;"></i>
        View Docs (Coming Soon)
      </button>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', async () => {
    await loadProfile();
    setupForms();
  });
  
  async function loadProfile() {
    try {
      const response = await API.get('/auth/me');
      const supplier = response.supplier;
      
      document.getElementById('companyName').value = supplier.companyName || '';
      document.getElementById('companyCode').value = supplier.companyCode || '';
      document.getElementById('contactName').value = supplier.contactName || '';
      document.getElementById('email').value = supplier.email || '';
      document.getElementById('phone').value = supplier.phone || '';
      document.getElementById('website').value = supplier.website || '';
      
      // Load usage stats
      try {
        const dashboardResponse = await API.get('/dashboard');
        document.getElementById('usageParts').textContent = formatNumber(dashboardResponse.totalParts || 0);
        document.getElementById('usageFiles').textContent = dashboardResponse.files?.length || 0;
        document.getElementById('usageBrands').textContent = dashboardResponse.uniqueBrands || 0;
        document.getElementById('usageInStock').textContent = formatNumber(dashboardResponse.inStockCount || 0);
      } catch (err) {
        // Ignore stats errors
      }
    } catch (err) {
      console.error('Failed to load profile:', err);
      showToast('Failed to load profile', 'error');
    }
  }
  
  function setupForms() {
    document.getElementById('profileForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const form = e.target;
      
      try {
        await API.put('/auth/profile', {
          companyName: form.companyName.value,
          contactName: form.contactName.value,
          phone: form.phone.value,
          website: form.website.value
        });
        
        showToast('Profile updated successfully', 'success');
      } catch (err) {
        showToast('Failed to update profile: ' + err.message, 'error');
      }
    });
    
    document.getElementById('passwordForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const form = e.target;
      const currentPassword = form.currentPassword.value;
      const newPassword = form.newPassword.value;
      const confirmPassword = form.confirmPassword.value;
      
      if (!currentPassword || !newPassword) {
        showToast('Please fill in all password fields', 'error');
        return;
      }
      
      if (newPassword !== confirmPassword) {
        showToast('New passwords do not match', 'error');
        return;
      }
      
      if (newPassword.length < 8) {
        showToast('New password must be at least 8 characters', 'error');
        return;
      }
      
      try {
        await API.put('/auth/password', {
          currentPassword,
          newPassword
        });
        
        showToast('Password changed successfully', 'success');
        form.reset();
      } catch (err) {
        showToast('Failed to change password: ' + err.message, 'error');
      }
    });
  }
</script>

<%- include('partials/footer') %>
